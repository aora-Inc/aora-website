# Netlify Redirects

# JSONファイルは常に直接配信（リダイレクトしない）
/content/*.json  200!
/*.json  200!

# Admin ページ（CMSアクセス用）
/admin/*  /admin/index.html  200

# フォーム送信後のサクセスページ
/success  /success.html  200
/success-en  /success-en.html  200

# SPA fallback - HTML以外のすべての404を index.html にリダイレクト
# 注意: JSONファイルは上記のルールで除外されている
/*    /index.html   200
